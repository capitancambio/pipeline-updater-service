# pipeline-updater-service
service for storing pipeline releases.

It relies on a mongolab free storage connection. This can be deployed quite straigh forward to heroku, googleapps, or just in a aws instance
